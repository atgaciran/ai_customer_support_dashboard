/**
 * @file next.config.ts
 * @description Next.js configuration file.
 * Currently uses default settings. Custom configuration options (e.g., image domains,
 * redirects, rewrites, webpack overrides) can be added to the nextConfig object below.
 * @see https://nextjs.org/docs/app/api-reference/config/next-config-js
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Add custom Next.js configuration options here as needed */
};

export default nextConfig;
